# Manifest: `walls_pillars_sgraphs_standard_optimized_old_001`

## Status

Blocked because the original rosbag is truncated and contains no recoverable
message rows.

## Available Files

| File | Purpose |
|---|---|
| `README.md` | Evidence and reason the workflow cannot run |
| `MANIFEST.md` | Result status |

The empty `plots/` and `sgraphs_topics_metadata/` directories are retained to
match the requested result structure. The bundle contains this documentation
only.
